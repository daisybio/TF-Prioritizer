import { TranscriptionFactor } from './transcription-factor';

describe('TranscriptionFactor', () => {
  it('should create an instance', () => {
    expect(new TranscriptionFactor()).toBeTruthy();
  });
});
